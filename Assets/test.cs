using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour {

	// Use this for initialization
	void Start () {
		float result = Mathf.Lerp (2f, 5f, 0.5f);
		Debug.Log(result);
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
